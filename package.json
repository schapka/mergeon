{
  "name": "mergeon",
  "version": "0.1.0",
  "description": "tbd",
  "author": "René Schapka",
  "license": "MIT",
  "main": "dist/mergeon.js",
  "bin": "dist/cli.js",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "microbundle build --compress false --format cjs src/{index,cli}.js",
    "pretest": "npm run -s build",
    "test": "node scripts/tasks/test.js"
  },
  "files": [
    "src",
    "dist"
  ],
  "devDependencies": {
    "glob": "^7.1.2",
    "microbundle": "~0.2.4",
    "rimraf": "~2.6.2"
  },
  "dependencies": {
    "chalk": "^2.3.0",
    "lodash": "^4.17.4",
    "yargs": "^10.0.3"
  }
}
